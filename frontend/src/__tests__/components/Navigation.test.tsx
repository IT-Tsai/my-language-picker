import Navigation from "@/components/Navigation";
import { render } from "@testing-library/react";
import { describe } from "node:test";

describe('Test Navigation Component', () => {

  it('test render', () => {
    const navCmp = render(<Navigation />);

  })

})